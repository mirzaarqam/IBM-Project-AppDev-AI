import tkinter as tk

# Create a new tkinter window
window = tk.Tk()

# Add a label to the window
label = tk.Label(window, text="Hello, Arqam. This is GUI Display on console based env!")
label.pack()

# Start the GUI event loop
window.mainloop()